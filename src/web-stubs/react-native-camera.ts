import React from 'react';
import { View } from 'react-native';

export const RNCamera = { Constants: { Type: { back: 'back', front: 'front' } } };
const Camera = (props: any) => React.createElement(View, props);
export default Camera;
